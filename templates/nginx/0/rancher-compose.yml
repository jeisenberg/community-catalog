.catalog:
  name: "Nginx"
  version: "v1.0"
  description: "Nginx"
  uuid: nginx-0
  minimum_rancher_version: v1.12.0
  questions:
    - variable: "NGINX_PORT"
      label: "nginx port"
      description: "i.e. 80"
      default: 80
      required: true
      type: "int"
    - variable: "PROXY_PORT"
      label: "nginx port"
      description: "i.e. 8080"
      default: 8080
      required: false
      type: "int"
nginx:
  scale: ${SCALE}
